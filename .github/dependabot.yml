version: 2
updates:
  # Keep GitHub Actions pinned versions (actions/checkout@v4, etc.) current.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    groups:
      actions:
        patterns: ["*"]

  # Uncomment and adjust for whichever package manager this project actually uses.
  # - package-ecosystem: "npm"
  #   directory: "/"
  #   schedule:
  #     interval: "weekly"
    # groups:
    #   actions:
    #     patterns: ["*"]

  # - package-ecosystem: "pip"
  #   directory: "/"
  #   schedule:
  #     interval: "weekly"
    # groups:
    #   actions:
    #     patterns: ["*"]

  # - package-ecosystem: "composer"
  #   directory: "/"
  #   schedule:
  #     interval: "weekly"
    # groups:
    #   actions:
    #     patterns: ["*"]
